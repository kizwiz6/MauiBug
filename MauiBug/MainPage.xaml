<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiBug.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a submarine number ten" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="Go to Video Page"
                SemanticProperties.Hint="Navigates to a page with a MediaElement"
                Clicked="OnGoToVideoClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="Force GC (tap after returning from video)"
                SemanticProperties.Hint="Forces garbage collection to trigger finalizer crash"
                Clicked="OnForceGCClicked"
                HorizontalOptions="Fill"
                BackgroundColor="Red"
                TextColor="White" />

            <Button
                Text="Auto Test (navigate + dispose + GC)"
                SemanticProperties.Hint="Programmatically navigates to video, comes back, and forces GC"
                Clicked="OnAutoTestClicked"
                HorizontalOptions="Fill"
                BackgroundColor="DarkOrange"
                TextColor="White" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
